%h2== Encumbrances for third party
%p== #{link_to_with_rights 'Create a new encumberance',"/encumberances/new/#{@funding_line.id}",:class=>"add"}
- if @encumberances.blank?
  %p
    %i No encumbrances have been created yet.
- else
  %table.narrow.form{ :width => '100%' }
    %thead
      %tr
        %th
        %th id
        %th name
        %th amount
        %th date of commencement        
    %tbody
    - for encumberance in @encumberances
      %tr{ :class => cycle('odd','') }
        %td &nbsp;
        %td
          = encumberance.id
        %td
          = encumberance.name
        %td
          = encumberance.assigned_value
        %td
          = encumberance.date_of_commencement
    %tfoot
      %tr
        %td{ :colspan => 9 }