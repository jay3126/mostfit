%h1 Add new loan applications for new loan applicants
- if !@message.blank? && !@message[:error].blank?
  .error
    = "#{@message[:error]}"
    %br
%form{:action => url(:controller => :loan_applications, :action => :bulk_create), :method => :get}
  = partial 'lib/branch_center_selector'      

%br
%hr

= partial :fields
  
%br
%hr
  = partial :recent_list 