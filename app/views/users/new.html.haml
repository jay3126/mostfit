
.prefix-2.span-20.suffix-2.last
  %h1 Create a new user

  = error_messages_for @user

  = form_for(@user, :action => url(:users)) do
    = partial 'fields'
    %table
      %td
        = submit "create"
      %td
        %h5 Or
      %td
        = link_to 'Back', url(:users), :class => "grey_button"

  %p
    %br/
