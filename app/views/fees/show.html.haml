%h1
  Fee 
  == #{@fee.id} : #{@fee.name}
    
= @fee.description

%br
= link_to_with_rights 'Edit',  url(:edit_fee,  @fee)
|
= link_to_with_rights 'Back', url(:fees)

%div.tab_container
  %ul.tabs
    %li#history
      History
%div.tab
  = partial "audit_trails/list", :layout => false
  %input{:type => "hidden", :name => "_load_remote", :value => audit_trail_url}

