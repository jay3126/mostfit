%h1 Create a new staff member
- unless @message.blank?
  - if !@message[:error].blank?
    .flash.error
      = @message[:error].class == Array ? @message[:error].join('<br>') : @message[:error]

= form_for(@staff_member, :action => url(:staff_members)) do
  = partial :fields
  = submit "Create staff member"
  &nbsp;&nbsp;&nbsp;
  %b
    Or
  &nbsp;&nbsp;&nbsp;
  = link_to_with_rights "Cancel", url(:staff_members), :class => "grey_button"