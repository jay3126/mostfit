%h1 Create a new funder

= error_messages_for @funder

= form_for(@funder, :action => url(:funders)) do
  = partial :fields
  = submit "Create Funder"
  &nbsp;&nbsp;
  %b
    Or
  &nbsp;&nbsp;
  = link_to_with_rights "Cancel", url(:funders), :class => "grey_button"