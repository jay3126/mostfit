%h1 Create a new funding line for Funder: #{@funder.name}

= error_messages_for @funding_line

= form_for(@funding_line, :action => resource(@funder, :funding_lines)) do
  = partial :fields
  = submit "Create funding line"
  &nbsp;&nbsp;
  %b
    Or
  &nbsp;&nbsp;
  = link_to_with_rights "Cancel", url(:funders), :class => "grey_button"