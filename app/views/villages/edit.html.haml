%h1==  Village: <i>#{@village.name}</i>

= error_messages_for @village

= form_for(@village, :action => url(:village, @village)) do
  = partial :fields
  = submit "Update #{@village.name.capitalize}"
  &nbsp;&nbsp;
  %b
    Or
  &nbsp;&nbsp;
  = link_to 'Show', url(:village, @village), :class => "grey_button"
  &nbsp;&nbsp;
  |
  &nbsp;&nbsp;
  = link_to 'Back', url(:villages), :class => "grey_button"